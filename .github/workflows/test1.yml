name: demo-action-workflow

on:
  push:
    branches:
      - main


jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: checkout code
        uses: actions/checkout@v4

      - name: print message
        run: echo "hello! devops people"

      - name: run multi line script
        run: |
          echo "this is multi line script"
          ls -la
          pwd  

